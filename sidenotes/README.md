# sidenotes

Adapted from the [sidenotes](https://ctan.org/pkg/sidenotes) package to work with memoir:

  + Removed import of `changepage`
  + Removed `\sidecaption`, `\marginfigure`, and `\margintable`
